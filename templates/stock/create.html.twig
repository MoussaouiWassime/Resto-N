{% extends 'base.html.twig' %}

{% block title %}Ajout quantité{% endblock %}

{% block main %}
    {{ form_start(form) }}

        <div class="mb-3">{{ form_row(form.quantity) }}</div>
        <div class="mb-3">{{ form_row(form.measureUnit) }}</div>

        <button type="submit" class="btn btn-success">Valider</button>
        <a href="{{ path('app_stock_add_list', {'id': restaurant.id}) }}" class="btn btn-secondary">Annuler</a>

    {{ form_end(form) }}
{% endblock %}
