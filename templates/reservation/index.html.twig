{% extends 'base.html.twig' %}

{% block title %}Mes Réservations{% endblock %}

{% block main %}
    <div class="container mt-4 mb-5">

        <div class="d-flex justify-content-between align-items-center mb-4">
            <div class="d-flex gap-2">
                <a href="{{ path('app_profile_show') }}" class="btn btn-outline-secondary btn-sm">Mon profil</a>
                <a href="{{ path('app_home') }}" class="btn btn-outline-primary btn-sm">Réserver ailleurs</a>
            </div>
        </div>

        <div class="row">
            {% for reservation in reservations %}
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="card h-100 border-0 shadow-sm {{ reservation.status == 'A' ? 'opacity-75' : '' }}">

                        <div class="card-header bg-white border-bottom-0 pt-3 pb-0 d-flex justify-content-between align-items-start">
                            <h5 class="fw-bold text-primary mb-0">{{ reservation.restaurant.name }}</h5>

                            {% if reservation.status == 'A' %}
                                <span class="badge bg-danger">Annulée</span>
                            {% elseif reservation.status == 'T' %}
                                <span class="badge bg-success">Terminée</span>
                            {% else %}
                                <span class="badge bg-primary">Confirmée</span>
                            {% endif %}
                        </div>

                        <div class="card-body">
                            <div class="d-flex flex-column gap-2 mt-2">
                                <div class="d-flex align-items-center text-muted">
                                    <span class="me-2">📅</span>
                                    <span class="fw-bold text-dark">{{ reservation.reservationDate|date('d/m/Y à H:i') }}</span>
                                </div>
                                <div class="d-flex align-items-center text-muted">
                                    <span class="me-2">👤</span>
                                    <span>{{ reservation.numberOfPeople }} couverts</span>
                                </div>
                                {% if reservation.restaurantTable %}
                                    <div class="d-flex align-items-center text-muted">
                                        <span class="me-2">🪑</span>
                                        <span class="text-success">Table n°{{ reservation.restaurantTable.number }}</span>
                                    </div>
                                {% endif %}
                            </div>
                        </div>

                        <div class="card-footer bg-light border-top-0 d-flex justify-content-between align-items-center">
                            <small class="text-muted">#{{ reservation.id }}</small>

                            {% if reservation.status != 'A' and reservation.status != 'T' %}
                                <div>
                                    <a href="{{ path('app_reservation_update', {id: reservation.id}) }}" class="btn btn-sm btn-outline-warning" title="Modifier">✏️</a>
                                    <a href="{{ path('app_reservation_cancel', {id: reservation.id}) }}" class="btn btn-sm btn-outline-danger" title="Annuler">✖</a>
                                </div>
                            {% else %}
                                <small class="fst-italic text-muted">Clôturée</small>
                            {% endif %}
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="col-12 text-center py-5">
                    <div class="fs-1 mb-3">📅</div>
                    <h3 class="text-muted">Aucune réservation pour le moment.</h3>
                    <a href="/" class="btn btn-primary mt-3">Trouver une table</a>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
