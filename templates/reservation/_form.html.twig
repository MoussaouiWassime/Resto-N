{% block main %}
    {{ form_start(form) }}

    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.reservationDate, {
                label: '📅 Date et Heure',
                attr: {class: 'form-control'}
            }) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.numberOfPeople, {
                label: '👤 Nombre de couverts',
                attr: {class: 'form-control', min: 1}
            }) }}
        </div>
    </div>

    {# Ces champs ne s'affichent que pour le restaurateur ou en édition #}
    {% if form.status is defined or form.restaurantTable is defined %}
        <hr class="my-3">
        <h6 class="text-muted mb-3">Gestion (Restaurateur)</h6>
        <div class="row">
            {% if form.status is defined %}
                <div class="col-md-6">
                    {{ form_row(form.status, {label: 'Statut'}) }}
                </div>
            {% endif %}
            {% if form.restaurantTable is defined %}
                <div class="col-md-6">
                    {{ form_row(form.restaurantTable, {label: '🍽️ Table attribuée'}) }}
                </div>
            {% endif %}
        </div>
    {% endif %}

    <div class="d-grid mt-4">
        <button type="submit" class="btn btn-primary btn-lg shadow-sm">
            {{ submit_label|default('📅 Confirmer la réservation') }}
        </button>
    </div>

    {{ form_rest(form) }}
    {{ form_end(form) }}
{% endblock %}
