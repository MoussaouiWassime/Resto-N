{% block main %}
            {{ form_start(form) }}

    <div class="mb-3">{{ form_row(form.productName) }}</div>
    <div class="mb-3">{{ form_row(form.category) }}</div>
    <div class="mb-3">{{ form_row(form.quantity) }}</div>
    <div class="mb-3">{{ form_row(form.measureUnit) }}</div>

    <button class="btn btn-success">Valider</button>
    <a href="{{ path('app_stock_index', {'id': restaurant.id}) }}" class="btn btn-secondary">Retour</a>


    {{ form_end(form) }}
{% endblock %}
