<div class="card h-100 border-0 shadow-sm">
    <div class="ratio ratio-16x9 position-relative">
        {% if restaurant.image %}
            <img src="{{ asset('images/restaurants/' ~ restaurant.image) }}" class="card-img-top object-fit-cover rounded-top" alt="{{ restaurant.name }}">
        {% else %}
            <div class="bg-light d-flex align-items-center justify-content-center text-muted w-100 h-100">
                <span class="fs-1">🍽️</span>
            </div>
        {% endif %}

        <span class="position-absolute top-0 end-0 m-2 badge bg-white text-dark shadow-sm">
            ⭐ 4.5
        </span>
    </div>

    <div class="card-body d-flex flex-column p-3">
        <h5 class="card-title fw-bold text-truncate mb-1">{{ restaurant.name }}</h5>
        <p class="card-text small text-muted text-truncate mb-3">{{ restaurant.description }}</p>

        <div class="mt-auto d-grid gap-2">
            <div class="d-flex gap-2">
                <a href="{{ path('app_order_create', {id: restaurant.id}) }}" class="btn btn-primary btn-sm flex-grow-1">Commander</a>
                <a href="{{ path('app_reservation_create', {id: restaurant.id}) }}" class="btn btn-outline-secondary btn-sm">Réserver</a>
            </div>
            <a href="{{ path('app_restaurant_show', {id: restaurant.id}) }}" class="btn btn-light btn-sm text-muted">Détails →</a>
        </div>
    </div>
</div>
